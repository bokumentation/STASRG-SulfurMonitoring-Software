import React from "react";

const LogsPage = ({ type, value, unit, period, status }) => {
    return (
        <div>
            <p>Hello this is Logs Pages</p>
        </div>
    );
};

export default LogsPage;
