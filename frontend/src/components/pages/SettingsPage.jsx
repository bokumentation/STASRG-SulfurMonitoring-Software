import React from "react";

const SettingsPage = ({ type, value, unit, period, status }) => {
    return (
        <div>
            <p>Hello this is Settings Pages</p>
        </div>
    );
};

export default SettingsPage;
